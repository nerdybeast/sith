(function() {
  function vendorModule() {
    'use strict';

    return {

      //This file was generated by ember cli with `self['simple-bar']` here but the object attached to the 
      //browser window when we imported simplebar.js (see ember-cli-build.js) is "SimpleBar", ex: `window.SimpleBar()`
      //"self" here is just an alias for "window" so we need to update to grab the correct property.
      
      //This line was => 'default': self['simple-bar'],
      'default': self['SimpleBar'],
      
      __esModule: true,
    };
  }

  define('simple-bar', [], vendorModule);
})();
